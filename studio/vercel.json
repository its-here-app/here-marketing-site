{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {"distDir": ".sanity/runtime"}
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": ".sanity/runtime/$1"
    }
  ]
}
